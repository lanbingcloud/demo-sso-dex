apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: source-pvc
  namespace: user-pipelines
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 10Mi
  volumeMode: Filesystem